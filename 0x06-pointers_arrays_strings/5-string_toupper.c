#include "holberton.h"

/**
 * string_toupper - and away we go
 * description: pointers
 * @string_toupper: variable
 * Return:
 */

char *string_toupper(char *)
{
        int y;
        int z = (_strlen(a) - 1);

        for (y = 0, n = z; y < n; y++, n--)
        {
                int z = a[y];

                a[y] = a[n];
                a[n] = z;
        }
}
