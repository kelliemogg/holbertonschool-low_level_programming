#include "holberton.h"

/**
 * _strlen - length of string
 * description: write a function
 * @s: string
 * Return: length of string
 */

int _strlen(char *s)
{
	int x;

	for (x = 0; s[x] != '\0'; ++x)
	{
	}
	return (x);
}
