#include "holberton.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * strtow - strtow
 * description: strtow
 * @str: string
 * Return: NULL
 */

char **strtow(char *str)
{
	if (str == NULL)
		return (NULL);

	if (str == "")
		return (NULL);
}
